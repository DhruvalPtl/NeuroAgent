[pytest]
# pythonpath ensures `from src.ingest.real_data import ...` works in tests
# without installing the package in editable mode.
pythonpath = .
addopts = --tb=short
testpaths = tests
markers =
    slow: marks tests that require ESM-2 model download or GPU (deselect with -m 'not slow')
